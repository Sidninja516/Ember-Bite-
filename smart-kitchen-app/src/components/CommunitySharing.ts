export class CommunitySharing {
    shareRecipe(recipe: string, community: string): void {
        console.log(`Sharing recipe: ${recipe} with community: ${community}`);
        // Logic to share the recipe with the specified community
    }

    joinCommunity(community: string): void {
        console.log(`Joining community: ${community}`);
        // Logic to join the specified community
    }
}